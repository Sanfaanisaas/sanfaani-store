import type { ReactNode } from "react";
export function LoadingState({ children = "Loading…" }: { children?: ReactNode }) { return <p role="status" aria-live="polite" className="rounded-2xl border border-navy-900/10 bg-white p-6 text-center text-sm text-mist">{children}</p>; }
export function EmptyState({ title, children }: { title: string; children?: ReactNode }) { return <section className="rounded-2xl border border-navy-900/10 bg-white p-8 text-center"><h2 className="font-display text-xl font-semibold text-ink">{title}</h2>{children && <p className="mt-2 text-sm text-mist">{children}</p>}</section>; }
export function ErrorState({ message, onRetry }: { message: string; onRetry?: () => void }) { return <section role="alert" className="rounded-2xl border border-red-200 bg-red-50 p-6 text-center text-sm text-red-900"><p>{message}</p>{onRetry && <button type="button" onClick={onRetry} className="mt-3 rounded-full border border-red-300 px-4 py-2 font-semibold">Try again</button>}</section>; }
