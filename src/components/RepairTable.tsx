import type { RepairQueueItem } from "@/lib/api/contracts";
export default function RepairTable({ repairs }: { repairs: RepairQueueItem[] }) { return <div className="overflow-x-auto"><table className="w-full text-left text-sm"><thead><tr><th>Repair</th><th>Status</th><th>Updated</th></tr></thead><tbody>{repairs.map((repair) => <tr key={repair.id}><td>{repair.id}</td><td>{repair.status}</td><td>{new Date(repair.updatedAt).toLocaleString()}</td></tr>)}</tbody></table></div>; }
