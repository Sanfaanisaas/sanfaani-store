import type { PublicQuote } from "@/lib/api/contracts";
export default function QuoteApprovalCard({ quote, onDecision, pending = false }: { quote: PublicQuote; onDecision: (decision: "approve" | "decline") => void; pending?: boolean }) { return <section className="rounded-xl border p-4"><h2 className="font-semibold">Quote version {quote.version}</h2><p className="mt-2">₦{quote.totalAmount.toLocaleString("en-NG")}</p><div className="mt-3 flex gap-2"><button type="button" disabled={pending} onClick={() => onDecision("approve")}>Approve</button><button type="button" disabled={pending} onClick={() => onDecision("decline")}>Decline</button></div></section>; }
