import type { Metadata } from "next";
import Link from "next/link";
import { guides } from "@/content/guides";
export const metadata: Metadata = { title: "Buyer guides", description: "Practical guides for choosing devices and preparing repairs." };
export default function BuyerGuidesPage() { return <main id="main-content" className="mx-auto max-w-5xl px-6 py-12"><h1 className="font-display text-4xl font-semibold">Buyer guides</h1><p className="mt-3 max-w-2xl text-mist">Clear, versioned guidance for purchase and repair decisions. Product availability, prices, and warranties remain subject to the applicable API response and policy.</p><div className="mt-8 grid gap-5 md:grid-cols-2">{guides.map((guide) => <article key={guide.slug} className="rounded-2xl border border-navy-900/10 bg-white p-6"><p className="text-xs font-semibold uppercase tracking-widest text-mist">Updated {guide.updatedAt}</p><h2 className="mt-2 font-display text-2xl font-semibold"><Link className="underline decoration-gold decoration-2 underline-offset-4" href={'/guides/' + guide.slug}>{guide.title}</Link></h2><p className="mt-3 text-sm text-mist">{guide.summary}</p><Link className="mt-5 inline-block text-sm font-semibold text-blue underline" href={'/guides/' + guide.slug}>Read guide</Link></article>)}</div></main>; }
