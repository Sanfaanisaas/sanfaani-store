import { useState } from "react";
import { View, Text, TextInput, Pressable, StyleSheet } from "react-native";
import { createCheckout } from "../src/api";
export default function Checkout(){ const [email,setEmail]=useState(''); const [state,setState]=useState(''); async function submit(){setState('Submitting…'); try{await createCheckout({paymentMethod:'paystack',shippingAddress:{street:'',city:'',state:'',country:'Nigeria'},items:[]},'mobile-'+Date.now());setState('Order submitted. The server will confirm payment and stock.')}catch{setState('Checkout unavailable or needs a current cart.')} } return <View style={styles.container}><Text accessibilityRole="header" style={styles.heading}>Checkout</Text><Text>Final price, availability, and payment are confirmed by the server.</Text><TextInput accessibilityLabel="Email" keyboardType="email-address" value={email} onChangeText={setEmail} placeholder="Email" style={styles.input}/><Pressable accessibilityRole="button" onPress={submit} style={styles.button}><Text>Place order</Text></Pressable>{state&&<Text accessibilityLiveRegion="polite">{state}</Text>}</View>}
const styles=StyleSheet.create({container:{flex:1,padding:24,gap:16},heading:{fontSize:28,fontWeight:'700'},input:{borderWidth:1,borderColor:'#ccd1d9',borderRadius:12,padding:12},button:{backgroundColor:'#b8892b',padding:14,borderRadius:24,alignItems:'center'}});
